services:
  - type: web
    name: nexus-h
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
      - key: NEXUS_H_DB_PATH
        value: /tmp/nexus_h.db
